import React from 'react';
import { View } from 'react-native';

// Simple web implementation of ConfettiCannon
const ConfettiCannon: React.FC<any> = ({ children }) => {
  return <View>{children}</View>;
};

export default ConfettiCannon;
